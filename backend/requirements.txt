# Build tools (required for building wheels) - install first
# These must be installed before other packages to avoid build errors
setuptools>=69.0.0
wheel>=0.43.0
pip>=24.0.0

# Django and REST Framework
Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.6.0

# Configuration and utilities
python-decouple==3.8
dj-database-url==2.2.0

# Database
psycopg2-binary==2.9.10

# Image processing
Pillow==10.4.0

# Security and sanitization
bleach==6.1.0

# Static files
whitenoise==6.8.2

# Media files (Cloudinary for production)
cloudinary==1.42.2
django-cloudinary-storage==0.3.0

# WSGI server
gunicorn==23.0.0
